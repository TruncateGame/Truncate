{
  "name": "word-definitions",
  "private": true,
  "scripts": {
    "start": "rm -f defs.db.gz && node build_sq.js && gzip defs.db"
  },
  "license": "CC-BY-SA",
  "dependencies": {
    "jsonlines": "^0.1.1"
  },
  "devDependencies": {
    "sqlite3": "^5.1.6"
  }
}